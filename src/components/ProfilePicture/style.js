import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 8,
    borderWidth: 3,
    borderRadius: 42,
    borderColor: 'red',
  },

  image: {
    borderWidth: 2,
    borderRadius: 40,
    borderColor: 'white',
  },
});

export default styles;
