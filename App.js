import React from 'react';
import {SafeAreaView, View, StyleSheet, Text} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <Text>Welcome to Instagram Clone</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});

export default App;
